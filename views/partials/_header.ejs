<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
      <% if (username) { %>
        <p class="ml-auto font-weight-bold" style="margin: 1em"> Logged in as: <%= username %> </p>
        <button type="logout" class="btn btn-outline-dark">Logout</button>
      <% } else { %>
      <form class="form-inline ml-auto" action="/login" method="POST">
          <input class="form-control" type="text" name="username" placeholder="Username" style="width: 300px; margin: 0.1em">
          <button type="login" class="btn btn-outline-dark">Login</button>
      </form>
    <% } %>
  </nav>
</header>